services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - db_data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/tracker.db
    restart: unless-stopped

volumes:
  db_data:
